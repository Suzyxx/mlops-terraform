# Python
__pycache__/
*.pyc
.venv/
venv/

# Model build artifacts (rebuilt by `python main.py`)
models/

# Note: data/ will be managed by DVC (it adds its own .gitignore entry on `dvc add`)
/data

# MLflow local tracking (experiments + registry live in sqlite/artifacts, not git)
mlflow.db
mlruns/
